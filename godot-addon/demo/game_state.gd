extends Node

# Demo autoload registered in project.godot. No other script mentions this
# singleton's name, so the doctor reports UNUSED_AUTOLOAD.
# (It is still a valid, referenced autoload, so it is NOT reported as an
# unused *script*.)
var score: int = 0
