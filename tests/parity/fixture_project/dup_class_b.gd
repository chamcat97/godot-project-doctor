class_name DupClass
extends Node

# Second declarer of DupClass -> DUPLICATE_CLASS_NAME (reported once, on the
# sorted-first file). Attached to a node in main_ok.tscn so neither file adds
# an UNUSED_SCRIPT finding on top.
